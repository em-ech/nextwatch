# Python 3.12, managed with uv (see .venv/).
# Exact pins for cross-machine reproducibility and a stable live demo (AUDIT M6).
# Frontend (web/) is managed separately via web/package.json (Node 24 / npm).
tensorflow==2.21.0
keras==3.14.1
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
matplotlib>=3.7
scipy>=1.11
# Serving (React frontend talks to this API; see web/)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # /import watchlist upload (multipart form)
requests>=2.31            # src/build_posters.py TMDB poster fetch (offline, one-time)
# Accounts / friends / Blend platform (stateful layer around the frozen model)
sqlalchemy>=2.0           # SQLite persistence (users, history, friendships)
bcrypt>=4.0               # password hashing (used directly; passlib is unmaintained)
pyjwt>=2.8                # JWT bearer auth
pydantic-settings>=2      # env-driven Settings (app/config.py)
